/* ============================================================
   شعار الكلية
   ------------------------------------------------------------
   كان الشعار مضمّناً base64 داخل App.jsx بنحو ٣٠ كيلوبايت من
   مصدر الملف، تدخل حزمة الجافاسكربت وتُحمَّل من جديد في كل زيارة.
   الملفات موجودة أصلاً في المستودع، فنقلها إلى public يجعل
   المتصفح يخزّنها مؤقتاً ويُخرجها من الحزمة.

   يُبنى المسار من BASE_URL لأن الموقع منشور تحت مسار فرعي
   (/NajahBCVideoPlatform/) لا على جذر النطاق.
   ============================================================ */

const base = import.meta.env.BASE_URL;

/** الشعار النبيذي — للخلفيات الفاتحة. */
export const LOGO_WINE = `${base}logo-wine.png`;

/** الشعار الأبيض — للخلفيات الداكنة كالترويسة والتذييل. */
export const LOGO_WHITE = `${base}logo-white.png`;

export const LOGO_ALT = "شعار كلية الأعمال والاتصال — جامعة النجاح الوطنية";
